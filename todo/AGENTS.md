# Todo 維護規則

Todo 有三種狀態：

- 未實作
- 已實作待審核
- 歷史紀錄

Agent 實作完成後，只能將項目標記為「已實作待審核」，並保留在
`todo.md`，不可自動搬入歷史紀錄。

每完成一個 Todo，必須先建立一個只包含該 Todo 相關變更的獨立 commit，
再開始實作下一個 Todo。

只有收到人工明確指令後，才能將待審核項目移到
`歷史紀錄/todo-歷史紀錄.md`。搬移時需保留原本的需求分組，將相關圖片移到
`歷史紀錄/圖片/` 並接續編號，再將參考圖連結更新為 `圖片/image-XX.png`。
